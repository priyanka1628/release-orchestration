apiVersion: v1
kind: Service
metadata:
  name: release-demo-active
  namespace: dev
spec:
  selector:
    app: release-demo
  ports:
  - port: 80
    targetPort: 3000
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: release-demo-preview
  namespace: dev
spec:
  selector:
    app: release-demo
  ports:
  - port: 80
    targetPort: 3000
  type: ClusterIP